# Highload Benchmark: NestJS + Vue 3 + PostgreSQL

Тестовое задание, демонстрирующее оптимизацию высоконагруженного эндпоинта с использованием In-Memory Cache и нагрузочное тестирование.

## 🚀 Быстрый старт (Docker)

1. Убедитесь, что Docker установлен.
2. Запустите проект одной командой:

\`\`\`bash
docker-compose up --build
\`\`\`

3. Откройте приложения:
   - **Frontend:** [http://localhost:8080](http://localhost:8080)
   - **Backend API:** [http://localhost:3000](http://localhost:3000)
   - **Swagger Docs:** [http://localhost:3000/api/docs](http://localhost:3000/api/docs)

> **Примечание:** При первом запуске бэкенд автоматически заполнит базу данных (50 000 записей). Это займет пару секунд.

---

## 📊 Результаты оптимизации

Была реализована стратегия оптимизации "Hot Data" (кэширование популярных страниц).

### Сравнение производительности (RPS - Requests Per Second)

| Режим теста | Стратегия БД (Cold) | Стратегия Кэш (Hot) | Прирост |
|-------------|---------------------|---------------------|---------|
| **Браузер** | ~48 RPS             | ~65 RPS             | **x1.3** |
| **Сервер**  | ~100 RPS            | ~3500 RPS           | **x35.0** |

### Анализ результатов

1. **Тест из Браузера:**
   Прирост производительности ограничен пропускной способностью браузера (макс. 6 одновременных соединений на домен) и накладными расходами HTTP/TCP. Даже при мгновенном ответе сервера, сеть вносит существенные задержки.

2. **Тест на Сервере (Internal Benchmark):**
   Исключает сетевые задержки и ограничения браузера.
   - **БАЗА (SQL):** Упирается в IO диска базы данных (~10ms на запрос).
   - **КЭШ (RAM):** Данные отдаются из оперативной памяти (<1ms), что дает колоссальный прирост производительности (в 30-40 раз).

---

## 🛠 Технологический стек

- **Backend:** NestJS, TypeORM, In-Memory Cache (cache-manager).
- **Database:** PostgreSQL 15.
- **Frontend:** Vue 3 (Composition API), Vite, Charting logic.
- **DevOps:** Docker, Docker Compose (Multistage builds).

## 📂 Структура репозитория

- `/backend` - Исходный код API
- `/frontend` - Исходный код Vue приложения
- `docker-compose.yml` - Оркестрация контейнеров